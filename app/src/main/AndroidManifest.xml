<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nirronyhossain.smarthouserent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationForm" />
        <activity android:name=".HomePage" />
        <activity android:name=".RegistrationForm1" />
        <activity android:name=".RentalList" />
        <activity android:name=".RentalDetails" />
        <activity android:name=".LandLoderAddPost" />
        <activity android:name=".PostList" />
        <activity android:name=".AddPostList" />
        <activity android:name=".AddPostDetails"></activity>
    </application>

</manifest>